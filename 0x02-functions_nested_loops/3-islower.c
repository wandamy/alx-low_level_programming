#include "main.h"
/**
 * _islower - checks if c is in lowercase and returns 1 if not returns 0
 * @c - value for letter c
 * return - 1 or 0 success
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
