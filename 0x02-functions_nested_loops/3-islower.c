#include "main.h"
/**
 * _islower - checks if c is in lowercase and returns 1 if not returns 0
 * @c: character argument
 * Return: 1 and 0 depending on condition
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
